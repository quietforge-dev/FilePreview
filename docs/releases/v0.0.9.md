# FilePreview v0.0.9

## 新增

- 支持在资源管理器中拖拽文件或文件夹到目标文件夹。
- 移动操作复用工作区边界校验，禁止移动工作区根目录、移动到自身或子目录，以及覆盖同名目标。
- 移动后同步刷新目录树、目录缓存、标签页、预览和 Markdown 编辑会话中的路径。

## 改进

- 针对 Windows WebView2 增加应用内指针拖拽处理，提升桌面端拖拽兼容性。
- 增加移动操作的 Rust 单元测试，覆盖正常移动、自包含目录和同名目标冲突场景。
