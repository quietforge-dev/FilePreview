# FilePreview 需求说明

## 1. 产品目标

FilePreview 是本地文件夹浏览和文件预览工具。用户打开一个文件夹后，在同一窗口内浏览目录、查看文件信息并预览文件内容。

本期目标是让用户完成以下操作：

1. 打开本地文件夹。
2. 在目录树和文件列表中定位文件。
3. 点击文件，在右侧直接查看内容。
4. 不支持的格式明确显示“暂不支持预览”。

应用只读，不提供文件编辑、移动、删除、重命名或同步能力。

## 2. 本期范围

### 2.1 工作区

- 通过“打开文件夹”选择一个本地目录作为工作区。
- 左侧显示工作区内的目录树和文件，可展开子目录；根目录默认只加载一层，展开文件夹时再加载其子项。
- 鼠标停留在文件上时显示名称、类型、大小和修改时间。
- 文件夹排在文件前，名称按字母顺序排列。
- 双击文件夹或在目录树中选择目录后，切换当前目录。
- 工具栏提供刷新当前目录的操作。
- 搜索仅过滤当前文件列表的文件名，不搜索文件内容，也不递归搜索子目录。
- 读取文件时必须限制在当前工作区内，不能通过路径跳转读取工作区外的文件。
- 工具栏提供“最近文件夹”和“浏览记录”菜单。
- “最近文件夹”显示最近打开的文件夹，点击记录可重新打开；支持清空记录。
- “浏览记录”显示最近预览过的文件，点击记录可重新打开所在文件夹并预览文件；支持清空记录。

### 2.2 预览区

- 选中文件后自动加载预览。
- 加载过程中显示加载状态；读取或解析失败时显示具体错误。
- 切换文件时应清理上一个图片预览使用的 Blob URL。
- 单个待预览文件最大为 25 MB，超过限制时提示用户。
- 预览区顶部显示文件名和扩展名。

### 2.3 本地数据库

- 使用 SQLite 保存工作区和文件浏览历史。
- 数据库文件名为 `filepreview.db`，放在当前用户的数据目录，不放在项目目录或安装目录。
- 工作区历史按最近打开时间倒序保存，文件历史按最近预览时间倒序保存；同一路径只保留一条记录。
- 历史记录默认最多显示最近 20 条。

### 2.4 支持的格式

| 类型 | 扩展名 | 预览要求 |
| --- | --- | --- |
| Markdown | `.md`、`.markdown`、`.mdx` | 按 Markdown 排版显示标题、段落、列表、引用、表格和代码块。 |
| 文本与代码 | `.txt`、`.json`、`.yaml`、`.yml`、`.xml`、`.toml`、`.ini`、`.env`、`.sql`、`.js`、`.ts`、`.tsx`、`.vue`、`.rs`、`.java`、`.kt`、`.go`、`.py`、`.sh`、`.css`、`.scss`、`.html`、`.csv`、`.log` | 只读等宽文本显示。 |
| PDF | `.pdf` | 使用 PDF 阅读器逐页渲染，支持预览区滚动查看。 |
| Office 文档 | `.doc`、`.docx`、`.ppt`、`.pptx`、`.xls`、`.xlsx` | 检测到 LibreOffice 后在本地转换为 PDF 并预览，允许与原始 Office 排版存在少量差异。 |
| 图片 | `.png`、`.jpg`、`.jpeg`、`.gif`、`.webp`、`.svg` | 在预览区内按比例显示。 |

以下格式在本期显示“暂不支持预览”：音视频、压缩包和其他二进制文件。

## 3. 预览器接口

文件格式判断和具体渲染必须分离。页面组件不得根据扩展名堆叠分支逻辑。

```ts
interface PreviewRenderer {
  readonly id: string;
  canHandle(file: FileInfo): boolean;
  render(file: FileInfo): Promise<PreviewContent>;
}
```

`PreviewManager` 按注册顺序选择第一个可处理当前文件的渲染器。当前注册的渲染器如下：

1. `MarkdownRenderer`
2. `OfficeRenderer`
3. `PdfRenderer`
4. `ImageRenderer`
5. `TextRenderer`
6. `UnsupportedRenderer`

新增格式时，应新增或扩展对应渲染器，不修改文件列表页面的格式判断逻辑。

## 4. 数据模型

前后端使用一致的文件信息结构：

```ts
interface FileInfo {
  path: string;
  name: string;
  extension: string;
  size: number;
  modifiedAt: number | null;
  isDirectory: boolean;
}
```

Rust 侧负责目录枚举和文件读取；Vue 侧负责选择渲染器和呈现内容。前端通过 Tauri Command 调用本地能力，不为本地文件操作额外引入 HTTP 服务。

## 5. 界面要求

窗口采用两栏布局：

```text
+----------------------+----------------------+
| 目录树与文件         | 文件预览             |
+----------------------+----------------------+
```

- 顶部工具栏包含应用名称、打开文件夹、刷新、当前路径和当前目录搜索。
- 左侧目录树用于目录导航和选择文件；目录树宽度可拖动调整。
- 右侧为常驻预览区；未选择文件时显示空状态。
- 搜索仅过滤左侧当前已加载目录中的文件名。
- 预览区支持滚动查看长图片和 PDF 文档。

## 6. 非功能要求

- 技术栈：Tauri 2、Rust、Tokio、SQLx、SQLite、Vue 3、TypeScript、Vite、Pinia、Element Plus、SCSS。
- 所有本地文件访问通过 Rust Tauri Command 执行。
- Markdown 和 DOCX 转换后的 HTML 必须进行清理，不能直接注入不可信 HTML。
- 读取目录和文件失败时返回可读错误，不使应用退出。
- 数据库结构通过 SQLx migration 管理，应用启动时自动执行未完成的 migration。
- 项目通过 `npm run build` 和 `cargo check --manifest-path src-tauri/Cargo.toml`。
- Windows 开发环境通过 `scripts/start_desktop.bat` 启动桌面端。

## 7. 验收标准

1. 用户可通过桌面端打开任意可访问的本地文件夹。
2. 打开后可看到目录树与第一层文件节点；展开或进入子目录后，相应节点按需加载并同步更新。
3. 选择 `.md`、`.txt`、`.pdf`、支持的 Office 文件或支持的图片文件时，右侧显示相应预览；Office 文件需要已安装 LibreOffice。
4. `.pdf` 与转换后的 Office 文档可逐页渲染，并支持在预览区滚动查看。
5. 选择未列出的格式时，右侧显示“暂不支持预览”，应用保持可用。
6. 工作区外路径和超过 25 MB 的文件不能被预览。
7. 双击 `scripts/start_desktop.bat` 能启动 Tauri 开发窗口。
8. 重新启动应用后，最近文件夹和浏览记录仍可从数据库加载。
9. 清空历史记录只删除数据库记录，不删除本地文件。

## 8. 后续迭代

### 8.1 已实现

以下能力已在当前版本实现，不再作为后续迭代事项：

- PDF 逐页预览，可在预览区滚动查看长文档。
- Excel 文件预览：安装 LibreOffice 后，`.xls`、`.xlsx` 在本地转换为 PDF 后预览。该能力不提供原生表格编辑、公式计算或单元格交互。
- 最近打开的工作区：界面中的“最近文件夹”菜单会持久化最近工作区；“浏览记录”会持久化最近预览的文件。
- 旧版 Office 格式兼容：`.doc`、`.ppt`、`.xls` 与新版 Office 格式均通过 LibreOffice 本地转换预览。
- 启动恢复：优先恢复上次会话中的活动标签；没有会话时自动打开最近一次工作区。
- 多标签页：支持工作区标签与文件预览标签，切换或关闭时释放当前预览资源，并持久化标签元数据用于下次恢复。
- 文件系统变更监听与自动刷新：监听活动工作区并合并短时间内的事件，自动刷新已加载目录；正在预览的文件被修改、删除或移动时会重新加载或显示不可用状态。
- 文件内容全文检索：递归搜索当前工作区内的文本、代码和 Markdown 文件，返回匹配行并可直接打开结果文件。

### 8.2 待迭代

#### 优先级 P0：预览性能与稳定性

- Office 转 PDF 结果缓存：以文件路径、大小和修改时间作为失效依据；同一未变更文件再次打开时直接读取缓存，避免重复启动 LibreOffice。
- Office 转换任务体验：显示转换阶段与耗时，支持切换文件时取消未完成任务，并为超时或转换失败提供可读错误和重试入口。
- PDF 阅读增强：缩放、页码跳转、文本搜索和适应宽度/页面控制；长 PDF 应按需渲染，避免一次性占用大量内存。

#### 优先级 P1：目录管理

- PDF 与 Office 文件的全文索引：在当前文本检索基础上，单独设计本地索引、增量更新和结果高亮方案；搜索范围、索引大小和隐私说明应由用户明确控制。
- 收藏目录：与“最近文件夹”分开保存，支持置顶、重命名显示名称和移除收藏。
- 文件列表效率：按名称、类型、大小、修改时间排序，按类型/时间/大小筛选，并在大目录中采用虚拟列表或分页加载。

#### 优先级 P2：预览交互与界面体验

- 图片缩放、旋转、适应窗口、原始尺寸和拖拽平移控制。
- 预览状态与布局持久化：记住目录树宽度、上次打开位置、展开目录和预览缩放状态。
- 深色主题与跟随系统主题。

#### 需单独立项的范围扩展

- 文件编辑、重命名、移动和删除：当前产品定位以浏览与预览为主；加入写操作前需补充确认、回收站、冲突处理和失败恢复设计。
- 远程文件系统：例如 SMB、SFTP、WebDAV 或对象存储；需单独处理连接凭据、安全存储、断线重连、缓存与权限模型。
